# iOSAudioAssignment
Records audio. The app have built-in noise detection for audio recording, alerting the user if noise levels exceed a certain threshold. After recording, the app reduces noise in the audio file. Users are able to play back and test the recorded media and delete the media.
